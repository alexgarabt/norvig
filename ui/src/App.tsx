import { CorpusLoader } from "@/components/corpus-loader";

export default function App() {
  return (
    <div className="min-h-screen bg-background font-sans">
      <CorpusLoader />
    </div>
  );
}
